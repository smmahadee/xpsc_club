#include <bits/stdc++.h>

#define ll long long

using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(NULL);

    int a, b;
    cin >> a >> b;

    if (b >= a) {
        cout << "Tyro" << endl;
    } else {
        cout << "Dom" << endl;
    }

    return 0;
}